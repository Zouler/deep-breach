# P1.7 — Command Pressure / Strategic Response

**Phase:** P1.7  
**Branch:** `feature/p1.7-command-pressure`  
**Last updated:** 2026-07-06  

---

## Narrative purpose

After successful **Growing Ocean Anomaly** passive monitoring, the phenomenon is no longer a dive-site problem alone. Command, Research, and Engineering disagree on DBX-07’s strategic posture. The player chooses how the base responds — without launching a new dive and without revealing the nature of the anomaly.

Tone: classified, procedural, restrained dread (COLD HULL). No aliens/monsters/explanation.

---

## Unlock condition

Command Pressure unlocks when:

- `growing_ocean_anomaly` is present in `completedSpineEvents` (successful Growing Ocean monitoring dive)
- `command_pressure` spine is **not** yet complete
- No strategic response flag is present

Helper: `isCommandPressurePending(state)` in `game/commandPressure.ts`

---

## Player choices (exactly one)

| Choice ID | Label | Story flag | Rewards |
|-----------|-------|------------|---------|
| `report_up_chain` | Report Up the Chain | `command_escalation` | +10 Research Data |
| `controlled_observation` | Continue Controlled Observation | `controlled_observation` | +10 Scrap, +15 Research Data |
| `emergency_withdrawal` | Prepare Emergency Withdrawal Protocols | `emergency_withdrawal_protocols` | +20 Scrap |

Resolution: `resolveCommandPressure(state, choice)` or reducer action `RESOLVE_COMMAND_PRESSURE`.

---

## Flags / spine

| Entry | When |
|-------|------|
| `command_pressure` (spine) | Once, on any valid choice |
| `command_escalation` | Report Up the Chain only |
| `controlled_observation` | Controlled Observation only |
| `emergency_withdrawal_protocols` | Emergency Withdrawal only |

Mutually exclusive strategic flags — second resolution is a no-op.

---

## Aftermath

On resolution:

- XO headline + department lead posture notes (Research, Engineering)
- Roberts personal acknowledgment beat
- Story beats logged to Captain’s Log
- Modest scrap/research applied to `resources` and `baseStorage`

---

## Future hook

After P1.7, **Abyssal Expansion Models** unlocks (`abyssal_expansion_models`) — see [p1.8-abyssal-expansion-models.md](./p1.8-abyssal-expansion-models.md).

No next dive implemented in P1.7.

---

## UI wiring

| Surface | Behavior |
|---------|----------|
| `app/base.tsx` | Banner when `isCommandPressurePending` |
| `app/mission-select.tsx` | Operational assignment card + lock copy |
| `app/story-mission-briefing.tsx` | Briefing + three choice buttons |
| `app/settings.tsx` | `__DEV__` QA fast-forward |

---

## QA helper

**Settings → Developer / QA → QA: Advance to Command Pressure ready**

Function: `advanceQaToCommandPressureReady()` in `game/qaProgression.ts`  
Reducer: `QA_FAST_FORWARD_TO_COMMAND_PRESSURE`  
Gated behind `__DEV__` only.

---

## Test coverage

`game/__tests__/commandPressure.test.ts`

- Unlock gating (before/after Growing Ocean)
- Decision uniqueness
- Per-choice rewards and flags
- Spine recorded once; no war/collapse spine
- Abyssal Expansion Review unlock after resolution
- QA helper + reducer action
- Save v6 compatibility (no migration bump)

---

## Files

| File | Role |
|------|------|
| `game/commandPressure.ts` | Core logic |
| `game/canon.ts` | `command_pressure` spine id |
| `data/storyMissions.ts` | Mission definitions |
| `game/gameReducer.ts` | Actions |
| `game/qaProgression.ts` | QA fast-forward |
