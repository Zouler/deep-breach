# P1.9 — Engineering Stress Response

**Phase:** P1.9  
**Branch:** `feature/p1.10-playtest-hardening`  
**Last updated:** 2026-07-06  

---

## Narrative purpose

After **Abyssal Expansion Models** (P1.8), Chief Engineer reports Mk I shielding and engine systems accumulating stress beyond nominal models. The player declares an engineering posture — a base-side briefing, not a dive.

Tone: classified, procedural, survivability-focused. No anomaly explanation or next descent authorization.

---

## Unlock condition

P1.9 unlocks when:

- `abyssal_expansion_models` is in `completedSpineEvents`
- Exactly one P1.8 model priority flag is present:
  - `model_current_drift` OR
  - `model_resonance_field` OR
  - `model_biological_contamination`

Helper: `isEngineeringStressResponsePending(state)` in `game/engineeringStressResponse.ts`

Mission id: `engineering_stress_response`

---

## Player choices (exactly one)

| Choice ID | Label | Story flag | Rewards |
|-----------|-------|------------|---------|
| `reinforce_hull_tolerance` | Reinforce Hull Tolerance | `engineering_hull_tolerance` | +15 Scrap |
| `shield_sensor_stack` | Shield Sensor Stack | `engineering_sensor_stack` | +15 Research |
| `preserve_repair_supplies` | Preserve Repair Supplies | `engineering_repair_supplies` | +10 Scrap, +10 Research |

Resolution: `resolveEngineeringStressResponse(state, choice)` or `RESOLVE_ENGINEERING_STRESS_RESPONSE`.

---

## Flags / spine

| Entry | When |
|-------|------|
| `engineering_stress_response` (spine) | Once, on any valid engineering posture |
| `engineering_hull_tolerance` | Reinforce Hull Tolerance only |
| `engineering_sensor_stack` | Shield Sensor Stack only |
| `engineering_repair_supplies` | Preserve Repair Supplies only |

Mutually exclusive engineering flags — second resolution is a no-op.

---

## P1.8 model priority flavor

Briefing and debrief text varies by P1.8 model flag via `modelPriorityFlavorForState()`.

Text-only branching — no gameplay systems beyond flags and copy.

---

## Future hook

After P1.9, placeholder **Descent Authorization Hold** unlocks (`descent_authorization_hold`):

> Deployment profile not yet authorized.

No next dive implemented in P1.9.

---

## UI wiring

| Surface | Behavior |
|---------|----------|
| `app/base.tsx` | Banner when `isEngineeringStressResponsePending` |
| `app/mission-select.tsx` | Operational assignment card + lock copy |
| `app/story-mission-briefing.tsx` | Model context, three choice buttons |
| `app/settings.tsx` | `__DEV__` QA fast-forward |

---

## QA helper

**Settings → Developer / QA → QA: Advance to Engineering Stress Response ready**

Function: `advanceQaToEngineeringStressResponseReady()`  
Reducer: `QA_FAST_FORWARD_TO_ENGINEERING_STRESS`  
Resolves P1.8 with current drift; leaves engineering posture pending.

---

## Test coverage

- `game/__tests__/engineeringStressResponse.test.ts`
- `game/__tests__/storyProgressionChain.test.ts` (P1.10 regression block)

---

## Files

| File | Role |
|------|------|
| `game/engineeringStressResponse.ts` | Core logic |
| `game/canon.ts` | `engineering_stress_response` spine id |
| `data/storyMissions.ts` | Mission + descent authorization hold |
| `game/gameReducer.ts` | Actions |
| `game/qaProgression.ts` | QA fast-forward |
